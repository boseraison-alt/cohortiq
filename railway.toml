[deploy]
startCommand = "npx prisma db push && npm start"
# Extend the proxy timeout beyond Railway's 5-minute default so that
# long Rich Video generations (Claude + TTS + FFmpeg) don't get killed
# by the edge proxy. 900s = 15 minutes.
healthcheckTimeout = 900
numReplicas = 1
