[phases.setup]
# Install fonts via apt so Sharp/librsvg can render SVG text in slide images.
# aptPkgs puts fonts in /usr/share/fonts/ which is where the bundled librsvg looks.
aptPkgs = ["fonts-dejavu-core", "fonts-freefont-ttf"]
